# @rach/llm

LLM gateway + provider adapters + metering hook.

**Status:** skeleton (Stage 1). Depends on `@rach/core` (workspace link).

**Planned runtime deps** (added when source is ported in):
- @anthropic-ai/sdk

See `docs/RachDev_RachBase_Shared_Core_Spec.md` for the exact file mapping.

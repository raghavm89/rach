# rachdev-backend (placeholder)

Agent Builder API. Populated in Stage 3. Consumes `@rach/core`,
`@rach/identity`, `@rach/billing`, `@rach/llm`, and calls RachBase over the
deploy API. Owns: agent registry/runtime orchestration, agent chat.

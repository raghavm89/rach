# rachbase-backend (placeholder)

Cloud / BaaS API. Populated in Stage 3 (split backend). Will consume
`@rach/core`, `@rach/identity`, `@rach/billing`, `@rach/deploy`.
Owns: deployment, monitoring, tenants, VM assignment, expansion, plans,
containers (CaaS), the deploy agent.
